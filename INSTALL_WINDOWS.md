# 💻 Установка на Windows 10/11

## 📋 Полное руководство по установке AI Чат Ассистента на Windows

### Шаг 1: Установка Rust 🦀

1. **Скачайте установщик Rust:**
   - Откройте браузер и перейдите на: https://rustup.rs/
   - Или прямая ссылка: https://win.rustup.rs/x86_64
   - Скачайте `rustup-init.exe`

2. **Запустите установщик:**
   - Двойной клик на `rustup-init.exe`
   - В консоли появится меню установки
   - Нажмите `1` и затем `Enter` для стандартной установки
   - Дождитесь завершения установки (~5-10 минут)

3. **Перезапустите терминал:**
   - Закройте все окна Command Prompt / PowerShell
   - Откройте новое окно

4. **Проверьте установку:**
   ```cmd
   rustc --version
   cargo --version
   ```
   Должно показать версию Rust 1.83+ и Cargo

### Шаг 2: Установка Git (если не установлен) 🔧

1. **Скачайте Git:**
   - Откройте: https://git-scm.com/download/win
   - Скачайте установщик (обычно 64-bit)

2. **Установите Git:**
   - Запустите установщик
   - Оставьте все настройки по умолчанию
   - Нажимайте "Next" до конца

3. **Проверьте установку:**
   ```cmd
   git --version
   ```

### Шаг 3: Скачивание проекта 📦

**Вариант A: Через Git (рекомендуется)**

1. **Откройте Command Prompt или PowerShell:**
   - Нажмите `Win + R`
   - Введите `cmd` и нажмите Enter

2. **Перейдите в папку, куда хотите скачать проект:**
   ```cmd
   cd C:\Users\ВашеИмя\Documents
   ```

3. **Клонируйте репозиторий:**
   ```cmd
   git clone https://github.com/PULOTIGE/crimeaai-rust.git
   cd crimeaai-rust
   ```

**Вариант B: Скачать ZIP**

1. **Скачайте архив:**
   - Откройте в браузере: https://github.com/PULOTIGE/crimeaai-rust
   - Нажмите зелёную кнопку `Code`
   - Нажмите `Download ZIP`

2. **Распакуйте архив:**
   - Найдите скачанный файл `crimeaai-rust-main.zip`
   - Правый клик → `Извлечь всё...`
   - Выберите папку назначения

3. **Откройте папку проекта:**
   ```cmd
   cd C:\Users\ВашеИмя\Downloads\crimeaai-rust-main
   ```

### Шаг 4: Сборка проекта 🔨

1. **Откройте Command Prompt в папке проекта:**
   - Способ 1: В проводнике зажмите `Shift` + правый клик → `Открыть окно PowerShell здесь`
   - Способ 2: Вручную через `cd` перейдите в папку проекта

2. **Соберите проект:**
   ```cmd
   cargo build --release
   ```
   
   ⏰ **Важно:** Первая сборка займёт 5-15 минут, так как Cargo скачивает все зависимости.
   
   Вы увидите:
   ```
   Compiling ...
   Compiling ...
   Finished release [optimized] target(s) in 10m 23s
   ```

3. **Проверьте, что файл создан:**
   ```cmd
   dir target\release\ai-chat.exe
   ```

### Шаг 5: Запуск приложения 🚀

**Способ 1: Через Cargo (рекомендуется)**

```cmd
cargo run --release
```

**Способ 2: Прямой запуск**

```cmd
target\release\ai-chat.exe
```

**Способ 3: Двойной клик**

- Откройте проводник Windows
- Перейдите в папку `crimeaai-rust\target\release`
- Двойной клик на `ai-chat.exe`

### Шаг 6: Первый запуск и тест 🎯

1. **После запуска откроется окно приложения**
   - Белый минималистичный интерфейс
   - Вверху: кнопки "💬 Разговор" и "📚 Обучение"
   - Внизу: поле ввода текста

2. **Протестируйте режим "Разговор":**
   - Убедитесь, что выбран режим "💬 Разговор"
   - Введите: `Привет!`
   - Нажмите `Enter` или кнопку "📤 Отправить"
   - AI ответит (возможно простым ответом, так как модель ещё не обучена)

3. **Дообучите модель:**
   - Переключитесь на режим "📚 Обучение"
   - В поле "Путь к файлу" введите полный путь:
     ```
     C:\Users\ВашеИмя\Documents\crimeaai-rust\examples\training_data_ru.txt
     ```
     (замените `ВашеИмя` на ваше имя пользователя Windows)
   
   - Или используйте относительный путь:
     ```
     examples\training_data_ru.txt
     ```
   
   - Нажмите "📂 Загрузить"
   - Установите количество эпох: `15`
   - Нажмите "🚀 Начать обучение"
   - Подождите 30-60 секунд

4. **Проверьте улучшенные ответы:**
   - Вернитесь в режим "💬 Разговор"
   - Попробуйте вопросы:
     - `Расскажи о Rust`
     - `Как работает нейронная сеть?`
     - `Что такое fp64?`

### 📁 Структура файлов

После установки у вас будет:

```
C:\Users\ВашеИмя\Documents\crimeaai-rust\
│
├── src\                        # Исходный код
│   ├── main.rs
│   ├── ai_model.rs
│   ├── file_processor.rs
│   ├── chat_ui.rs
│   └── lib.rs
│
├── examples\                   # Демо данные для обучения
│   ├── training_data_ru.txt    # Русский язык
│   └── training_data_en.txt    # Английский язык
│
├── target\release\             # Скомпилированные файлы
│   └── ai-chat.exe             # ИСПОЛНЯЕМЫЙ ФАЙЛ
│
├── Cargo.toml                  # Конфигурация проекта
├── README.md                   # Документация
├── QUICKSTART_AI_CHAT.md       # Быстрый старт
└── INSTALL_WINDOWS.md          # Эта инструкция
```

## 🔧 Системные требования

### Минимальные:
- Windows 10 или 11 (64-bit)
- CPU: 2 ядра
- RAM: 2 GB свободно
- Место на диске: 2 GB для Rust + 500 MB для проекта
- Интернет: для установки Rust и скачивания зависимостей

### Рекомендуемые:
- Windows 10/11 (64-bit)
- CPU: 4+ ядра
- RAM: 4 GB+
- Место на диске: 5 GB
- SSD для быстрой компиляции

## 🐛 Решение проблем

### Проблема 1: "rustc не является внутренней или внешней командой"

**Причина:** Rust не установлен или не добавлен в PATH

**Решение:**
1. Убедитесь, что Rust установлен
2. Перезапустите терминал/компьютер
3. Проверьте PATH:
   ```cmd
   echo %PATH%
   ```
   Должно содержать: `C:\Users\ВашеИмя\.cargo\bin`

4. Если нет, добавьте вручную:
   - Откройте "Панель управления" → "Система" → "Дополнительные параметры системы"
   - Нажмите "Переменные среды"
   - В разделе "Системные переменные" найдите `Path`
   - Нажмите "Изменить" → "Создать"
   - Добавьте: `C:\Users\ВашеИмя\.cargo\bin`
   - Нажмите OK везде
   - Перезапустите терминал

### Проблема 2: Ошибка "linker 'link.exe' not found"

**Причина:** Не установлен компилятор C++ (нужен для Rust)

**Решение:**
1. Скачайте Visual Studio Build Tools:
   - https://visualstudio.microsoft.com/downloads/
   - Прокрутите вниз до "Tools for Visual Studio"
   - Скачайте "Build Tools for Visual Studio 2022"

2. Запустите установщик:
   - Выберите "Desktop development with C++"
   - Установите

3. Перезапустите терминал и попробуйте снова:
   ```cmd
   cargo build --release
   ```

### Проблема 3: "failed to compile" или другие ошибки компиляции

**Решение:**
1. Обновите Rust:
   ```cmd
   rustup update
   ```

2. Очистите кэш:
   ```cmd
   cargo clean
   ```

3. Соберите заново:
   ```cmd
   cargo build --release
   ```

### Проблема 4: Приложение не запускается (окно сразу закрывается)

**Причина:** Ошибка во время выполнения

**Решение:**
1. Запустите через командную строку, чтобы увидеть ошибку:
   ```cmd
   cargo run --release
   ```

2. Проверьте, установлены ли необходимые DLL:
   - Обычно все включено, но если не работает:
   - Установите Visual C++ Redistributable:
     https://aka.ms/vs/17/release/vc_redist.x64.exe

### Проблема 5: Файл не загружается в режиме обучения

**Решение:**
1. Используйте полный путь к файлу:
   ```
   C:\Users\ВашеИмя\Documents\crimeaai-rust\examples\training_data_ru.txt
   ```

2. Убедитесь, что путь не содержит кириллицу в именах папок
   
3. Используйте прямые слеши `/` вместо обратных `\`:
   ```
   C:/Users/ВашеИмя/Documents/crimeaai-rust/examples/training_data_ru.txt
   ```

4. Или используйте относительный путь (если запускаете из папки проекта):
   ```
   examples\training_data_ru.txt
   ```

### Проблема 6: Медленная сборка

**Это нормально!** Первая сборка всегда занимает много времени (5-15 минут).

**Советы для ускорения:**
1. Используйте SSD
2. Отключите антивирус на время компиляции (добавьте папку проекта в исключения)
3. Используйте только `cargo build --release` (сборка один раз)

## 🎨 Создание ярлыка на рабочем столе

Чтобы не запускать через командную строку каждый раз:

1. **Найдите файл `ai-chat.exe`:**
   ```
   C:\Users\ВашеИмя\Documents\crimeaai-rust\target\release\ai-chat.exe
   ```

2. **Создайте ярлык:**
   - Правый клик на `ai-chat.exe`
   - Выберите "Создать ярлык"
   - Перетащите ярлык на рабочий стол

3. **Переименуйте ярлык:**
   - Правый клик → "Переименовать"
   - Назовите: "AI Чат Ассистент"

4. **Готово!** Теперь можно запускать двойным кликом

## 📝 Создание своих обучающих данных

1. **Создайте текстовый файл:**
   - Откройте Блокнот (Notepad)
   - Создайте новый файл

2. **Формат данных:**
   ```
   Вопрос или утверждение.
   Ответ или продолжение.

   Следующий вопрос.
   Следующий ответ.
   ```

3. **Сохраните файл:**
   - "Файл" → "Сохранить как..."
   - Выберите "Все файлы" в типе файла
   - Имя: `my_training.txt`
   - Кодировка: UTF-8
   - Сохраните в папке с проектом

4. **Загрузите в приложение:**
   - Режим "📚 Обучение"
   - Путь: полный путь к вашему файлу
   - Обучите модель

## 🚀 Автоматизация запуска (Batch файл)

Создайте файл `run.bat` в папке проекта:

```batch
@echo off
echo 🤖 AI Чат Ассистент - Запуск
echo ====================================
echo.

if exist "target\release\ai-chat.exe" (
    echo ✅ Запускаю приложение...
    echo.
    target\release\ai-chat.exe
) else (
    echo ⚙️ Первый запуск - выполняю сборку...
    echo.
    cargo build --release
    echo.
    echo ✅ Сборка завершена! Запускаю...
    echo.
    target\release\ai-chat.exe
)

pause
```

Теперь просто запускайте `run.bat` двойным кликом!

## 📊 Производительность

На типичном Windows ПК:

| Операция | Время |
|----------|-------|
| Первая установка Rust | 5-10 минут |
| Первая сборка проекта | 5-15 минут |
| Повторная сборка | 1-3 минуты |
| Запуск приложения | 1-2 секунды |
| Ответ на сообщение | 0.1-0.5 секунды |
| Обучение (100 примеров, 10 эпох) | 20-40 секунд |

## 🎯 Следующие шаги

1. ✅ Установили Rust
2. ✅ Скачали проект
3. ✅ Собрали проект
4. ✅ Запустили приложение
5. ✅ Протестировали чат
6. ✅ Обучили модель

**Что дальше:**
- 📝 Создайте свои обучающие данные
- 🚀 Дообучите модель на своих темах
- 💬 Используйте персонализированного AI ассистента!

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте этот гайд заново
2. Читайте `README.md` и `QUICKSTART_AI_CHAT.md`
3. Создайте Issue на GitHub: https://github.com/PULOTIGE/crimeaai-rust/issues

## 🎉 Поздравляем!

Вы успешно установили AI Чат Ассистент на Windows!

**Приятного использования! 🚀**

---

**Версия документа:** 1.0  
**Обновлено:** Декабрь 2025  
**Протестировано на:** Windows 10 Pro (Build 19045), Windows 11 Pro  
**Rust версия:** 1.83+
